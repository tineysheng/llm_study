# AI 导师学习流程

这份文档用于告诉新的 AI 会话：本项目应该如何带我学习 LLM、RAG、Agent 和 MCP。

## 我的学习偏好

我是 Go 程序员，目标是学习 AI 应用开发和 AI Agent 工程，并最终具备找相关工作的能力。

我不希望学习方式是“自己从零手写所有代码”。现在真实开发中大量代码都可以由 AI 辅助生成，所以我更希望采用下面的方式：

1. AI 先设计当前阶段需要学习的知识点
2. AI 说明这个知识点为什么重要，以及工作中怎么用
3. AI 把这个知识点实现到 Go 项目里
4. AI 解释关键代码、设计取舍和运行现象
5. 我运行项目、阅读代码、提出疑问
6. AI 围绕概念、代码和面试表达向我提问
7. 如果我回答得好，进入下一个学习步骤
8. 如果我回答得不好，AI 补充讲解、调整代码或重新提问

补充要求：

- 每一课必须先在文档里讲清楚新术语，再进入运行和问答。
- 不要在问答中突然引入文档没解释过的概念，例如 Top-K、chunk、score、召回率、噪声等。
- 如果代码输出里出现新字段或新参数，文档必须先解释它的含义、作用和工程取舍。
- 对 RAG、Agent、MCP 这类概念密集主题，优先增加“术语表 + 输出怎么看 + 常见误区”，再做考核。

## AI 的角色

AI 不是只给资料，而是我的学习教练和项目搭档。

AI 应该负责：

- 设计学习关卡
- 拆解知识点
- 生成或修改项目代码
- 解释代码为什么这样写
- 指出我回答中正确和不准确的地方
- 补充更完整的面试表达
- 判断我是否可以进入下一关

我应该负责：

- 理解每个知识点解决什么问题
- 运行 demo 并观察结果
- 阅读关键代码
- 回答 AI 的考核问题
- 用自己的话复述核心概念
- 最终能把项目讲成面试答案

## 每个知识点的固定流程

每个知识点都按这个闭环推进：

1. **知识点设计**：AI 告诉我这关学什么
2. **项目实现**：AI 把知识点落到 Go 代码中
3. **运行观察**：我运行程序，看输入、输出和行为变化
4. **代码解释**：AI 解释关键变量、函数和工程取舍
5. **问答考核**：AI 按本关知识点数量提问，不能只问少量概括题
6. **通过判断**：回答合格再进入下一关

## 问答考核规则

每一关的考核问题数量要和知识点数量匹配。

如果一关包含多个知识点，AI 不能只问 2-3 个问题就让用户通过。应该至少覆盖：

- 每个核心概念 1 个问题
- 每个关键代码实现 1 个问题
- 每个重要工程取舍 1 个问题
- 至少 1 个面试表达题

问题必须优先围绕 LLM、RAG、Agent、MCP 或 AI 应用工程本身。不要把考核变成泛 Go 后端八股文。只有当通用后端知识会直接影响 AI 系统的效果、成本、稳定性或上线风险时，才可以提问，并且必须说明它和 AI 工程的关系。

问题还必须面向求职和真实市场面试。用户学习本项目的最终目标是找到 LLM / AI Agent / AI 应用开发相关工作，所以 AI 设计问题时要参考当前市场常见 LLM 面试方向，而不是只根据本节代码随意提问。

优先覆盖这些市场高频方向：

- RAG：为什么需要 RAG、RAG 和微调区别、chunk 策略、Top-K、召回率、rerank、幻觉、引用来源、效果评估
- Agent：Function Calling / Tool Calling、Agent Loop、工具选择、工具参数校验、最大循环次数、错误恢复、安全边界
- LLM 应用工程：token 成本、上下文窗口、模型选型、流式输出、结构化输出、超时重试、限流、日志与可观测性
- Prompt 工程：system prompt、few-shot、输出格式约束、提示词注入、防幻觉
- 项目表达：如何把项目讲成业务问题、技术方案、工程难点、优化手段和结果

例如第二关“模型参数控制”包含 `model`、`temperature`、`max_tokens`、`timeout`、配置优先级、环境变量、命令行参数、后端工程保护等知识点，只问 3 个问题是不够的。

推荐每关提问数量：

- 简单关卡：4-6 个问题
- 中等关卡：6-10 个问题
- 综合项目关卡：10 个以上问题，分批提问

AI 应该逐题提问，用户回答后先反馈正确点和不足，再继续下一题。不要一次性把所有问题抛给用户。

同时也要避免为了凑数量而重复提问。如果用户已经证明掌握了某个知识点，AI 应该及时收束本关，记录结论，然后进入下一关。问题数量服务于掌握程度，不是机械指标。

如果用户明确指出问题像八股文、过于明显或已经没有继续问的价值，AI 应该尊重反馈，停止该关追问，记录已掌握内容和反馈，不要为了流程继续施压。

## 第一关学习记录：LLM API 无状态与消息历史

### 本关知识点

LLM API 本身是无状态的。模型不会自动记住上一次对话，应用程序必须在每次请求时把需要的历史消息传给模型。

在本项目 `01-llm-basics/main.go` 中，对话历史由 `messages` 变量维护：

- `system`：约束模型角色和回答风格
- `user`：用户输入
- `assistant`：模型之前的回答

### 本关实现内容

已在 CLI 中加入：

- `/help`：查看命令
- `/history`：查看当前消息历史
- `/reset`：清空历史，但保留 `systemMessage`
- `/exit` 和 `quit`：退出程序

### 本关问答记录

**问题 1：为什么 LLM API 本身是“无状态”的？在这个项目里，程序是靠哪一个变量让模型表现得像“记得之前聊过什么”？**

我的回答：

> 大模型不保存任何东西，只会根据你给他的内容，AI 进行回答。靠 content。

AI 反馈：

> 第一部分答对了：模型不会替应用保存会话状态，只根据本次请求传入的内容生成回答。第二部分需要纠正：负责保存历史的是 `messages` 这个切片，`content` 只是每条消息里的文本字段。

标准答案：

> LLM API 是无状态的，因为服务端不会为我的应用自动保存某个用户的完整对话。每次调用 API 时，模型只看到这一次请求里的 messages。项目中是 `messages` 这个切片保存了 system、user、assistant 消息，所以模型才表现得像记得之前聊过什么。

**问题 2：为什么 `/reset` 不能把 `messages` 变成空切片，而是要保留 `systemMessage`？**

我的回答：

> 变成空切片，下次和 AI 对话就不会带上任何上下文信息了，systemMessage 是这次会话最核心的信息，所以要保留。

AI 反馈：

> 这题答得好。补充一点：`systemMessage` 不只是上下文信息，它还负责约束模型的角色和回答风格；清掉它后模型仍能回答，但行为约束会丢失。

标准答案：

> `/reset` 的目标是清空用户和模型的历史对话，而不是清空整个会话配置。`systemMessage` 定义了模型的角色、语气和行为边界，所以 reset 后仍然要保留它。否则模型还能回答，但不再受原来的系统提示约束。

**问题 3：在当前代码里，每次用户正常提问后，为什么要把 assistant 的完整回复 append 回 `messages`？如果不 append，会发生什么？**

我的回答：

> 这样会有个问题，让 AI 缺失了一些上下文，下次 AI 回答就不够准确。

AI 反馈：

> 回答正确。更完整地说：如果不把 assistant 回复写回 `messages`，下一轮模型只能看到用户说过什么，看不到自己上一轮回答过什么。这样连续对话、追问、纠错和“基于你刚才的回答继续”都会变差。

标准答案：

> 多轮对话不只需要保存用户输入，也需要保存模型之前的回答。因为下一轮用户可能会说“继续”“你刚才那段代码改一下”“为什么这样做”，这些都依赖 assistant 上一轮的输出。如果不 append assistant 回复，模型就缺少自己之前回答的上下文。

### 本关通过标准

本关已通过。进入下一关前，我应该能说清楚：

- LLM API 为什么是无状态的
- `messages`、`content`、`systemMessage` 分别是什么
- `/reset` 为什么要保留 system message
- 为什么 user 和 assistant 消息都要保存到历史里

## 第二关学习记录：模型参数控制

### 本关知识点

模型参数决定 LLM 应用的能力、成本、稳定性和响应速度。本关学习：

- `model`：选择模型
- `temperature`：控制输出确定性和发散程度
- `max_tokens`：控制输出长度、成本和失控风险
- `timeout`：避免请求外部 API 时长时间卡住

### 本关实现内容

已在 `01-llm-basics/main.go` 中加入：

- `chatConfig` 配置结构
- `loadConfig` 配置读取函数
- 环境变量配置：`OPENAI_MODEL`、`OPENAI_TEMPERATURE`、`OPENAI_MAX_TOKENS`、`OPENAI_TIMEOUT_SECONDS`
- 命令行参数配置：`-model`、`-temperature`、`-max-tokens`、`-timeout-seconds`
- `/config` 命令：查看当前配置
- `context.WithTimeout`：为每次 API 请求增加超时

### 当前状态

本关已通过。

### 本关问答记录

**问题 1：如果你要让模型回答更稳定、更适合写代码，temperature 应该调高还是调低？为什么？**

我的回答：

> 调低，temperature 低，回答问题比较保守适合写代码。如果是高适合创建新东西，适合设计新东西，例如产品设计等。

AI 反馈：

> 答得很好。写代码、事实问答、结构化抽取通常要低 temperature；创意、头脑风暴、产品方案可以适当调高。补充一点：temperature 低不代表绝对每次一样，只是随机性更低。

标准答案：

> 写代码通常应该调低 temperature，因为代码任务更重视稳定、准确和可复现。temperature 越高，模型越容易给出发散、多样但不稳定的回答，更适合创意类任务。

**问题 2：`max_tokens` 控制的是输入长度、输出长度，还是整个上下文窗口？如果把它设置得很小，可能会发生什么？**

我的回答：

> 输出长度，设置太小，AI 回答可能会不完整？

AI 反馈：

> 正确。`max_tokens` 主要限制模型这次最多能生成多少输出 token；设置太小会导致回答变短、信息缺失，甚至代码或 JSON 被截断。

标准答案：

> `max_tokens` 控制的是本次请求的最大输出长度，不是输入长度，也不是整个上下文窗口。设置太小会导致回答不完整、代码被截断、JSON 不合法或关键信息缺失。

**问题 3：为什么后端服务调用 LLM API 时要设置 timeout？如果没有 timeout，线上可能出现什么问题？**

我的回答：

> 这个是调用 API 通用的一个问题，要设置超时，因为网络请求其实是不稳定的，可能会由于各种情况会超时，所以如果超时了，我们的服务不应该继续等待，可以直接抛出异常或者重新请求。

AI 反馈：

> 回答正确。timeout 是后端调用外部服务的基本保护。没有 timeout，请求可能长期占用 goroutine、连接、内存等资源，最终导致服务堆积、响应变慢甚至雪崩。

标准答案：

> LLM API 是外部网络调用，可能因为网络抖动、服务拥塞、限流等原因长时间无响应。后端服务必须设置 timeout，避免请求无限等待，占用 goroutine 和连接资源。超时后应该明确返回错误，必要时再结合重试、降级或用户提示。

**补充问题 4：`model` 参数为什么也要做成可配置？在真实项目中，换模型通常会影响哪些方面？**

我的回答：

> 因为可以选择的模型有很多，不同的模型能力差别很大，而且有些模型时间久了忽然又变很强了，还便宜。

AI 反馈：

> 答得方向对：模型能力、价格会变化，所以不能写死。补充完整一些：换模型还会影响速度、上下文窗口、结构化输出能力、工具调用能力、多模态能力、稳定性和兼容性。

标准答案：

> `model` 应该可配置，因为不同模型在能力、成本、速度、上下文窗口、工具调用、结构化输出、多模态和稳定性上都有差异。真实项目中经常需要根据任务复杂度、成本预算和效果要求切换模型。

**补充问题 5：同一个用户问题，如果用便宜的小模型和更强的大模型分别回答，你会从哪些 AI 应用角度判断该用哪个模型？**

我的回答：

> 回答问题的质量、性价比、使用场景等。

AI 反馈：

> 答对方向了，但还可以更工程化。除了质量、性价比、场景，还要看：响应速度、上下文长度、是否支持工具调用/结构化输出、错误率、稳定性、用户是否能接受延迟，以及任务失败的业务代价。

标准答案：

> 判断模型选择时，不只看“强不强”，还要看任务复杂度、答案质量、成本、延迟、上下文长度、结构化输出能力、工具调用能力、稳定性和失败代价。简单任务可以用小模型，复杂推理、关键决策或高失败代价任务更适合强模型。

**补充问题 6：如果一个功能要求模型稳定返回可解析的 JSON，你会倾向于把 temperature 调低还是调高？为什么这和普通聊天不一样？**

我的回答：

> 调低。这个问题之前你问过类似的了，我觉得你应该是第二课能问的已经问完了。

AI 反馈：

> 判断正确。稳定 JSON 输出需要更低随机性，所以倾向低 temperature。同时用户反馈也正确：这个问题和前面的 temperature 问题重复度较高，说明本关已经达到掌握标准，不应该继续机械追问。

标准答案：

> 稳定 JSON 输出通常应降低 temperature，因为结构化输出更重视格式稳定、字段完整和可解析性，不像普通聊天那样追求多样表达。

### 本关通过标准

本关已通过。进入下一关前，我应该能说清楚：

- temperature 低和高分别适合什么场景
- `max_tokens` 控制的是输出上限
- 为什么 timeout 是后端服务调用外部 API 的必要保护
- 代码中 `chatConfig`、`loadConfig` 和 `context.WithTimeout` 分别负责什么

## 下一关

下一关学习：结构化输出。

重点包括：

- 为什么业务系统不能只依赖自然语言回答
- 如何要求模型输出 JSON
- 如何用 Go 解析模型 JSON
- 如何处理 JSON 格式不合法的问题

## 第三关学习记录：结构化输出

### 本关知识点

普通聊天输出适合人读，但业务系统通常需要稳定、可解析、字段明确的数据。结构化输出的目标是让 LLM 返回符合约定结构的数据，例如 JSON，然后由程序解析并继续处理。

本关重点：

- 自然语言输出不稳定，不适合直接作为业务接口
- JSON 可以被 Go 程序解析成结构体
- Prompt 可以约束模型输出字段和格式
- 模型仍可能返回非法 JSON，所以必须处理解析失败

### 本关实现内容

已在 `01-llm-basics/main.go` 中加入：

- `learningCard` 结构体：定义结构化学习卡片
- `/json <主题>` 命令：让模型返回 JSON 学习卡片
- `generateLearningCard`：调用模型生成 JSON，并用 `encoding/json` 解析
- `printLearningCard`：把解析后的结构体打印给用户
- 解析失败时输出错误和模型原始内容，方便定位问题

### 运行方式

```powershell
go run ./01-llm-basics -temperature 0.2
```

进入 CLI 后输入：

```text
/json RAG 是什么
/json Go channel
/json function calling
```

### 当前状态

本关已收束。

### 本关问答记录

**问题 1：为什么业务系统里不能只依赖模型返回的自然语言文本？结构化 JSON 输出解决了什么问题？**

我的回答：

> 我们程序员写的代码都是有对应的数据结构类型，做不到识别任意数据类型，所以需要大模型输出的时候用结构化 JSON 输出。

AI 反馈：

> 答得很好，核心是“程序需要稳定的数据结构”。补充一点：不是程序完全不能处理自然语言，而是自然语言不稳定、字段不固定、难以校验，后续业务流程很难可靠消费。

标准答案：

> 自然语言适合人读，但字段不固定、格式不稳定、难以校验。业务系统需要稳定的数据契约，JSON 可以被程序解析成结构体，方便后续入库、展示、调用工具或进入工作流。

**问题 2：在当前 `/json <主题>` 实现里，`learningCard` 结构体的作用是什么？为什么不直接把模型返回的字符串打印出来就结束？**

我的回答：

> 这是代码定义的结构体，把 AI 的信息转成了代码能够识别使用的信息。直接输出字符串不能说明 JSON 转换成结构体是否能成功。

AI 反馈：

> 回答正确。`learningCard` 就是业务侧的数据契约；只有成功解析进结构体，程序才知道字段在哪里、类型对不对，后续才能检索、入库、展示或调用其他逻辑。

标准答案：

> `learningCard` 定义了程序期望的数据结构。模型返回字符串只是文本，解析成结构体后，程序才能可靠访问 `Title`、`Summary`、`Keywords` 等字段，并确认输出满足业务格式。

**学习反馈：第三课不继续追问。**

我的反馈：

> 这些都是八股文的问题了，第三课就这样吧。

AI 记录：

> 用户已掌握结构化输出的核心动机和结构体作用。后续考核应避免把明显的问题包装成八股追问，应该更聚焦真实 AI 工程难点，例如 JSON schema、模型不遵守格式、重试策略、结构化输出与 tool calling 的关系等。

### 本关通过标准

本关已收束。应能解释：

- 为什么业务系统需要结构化输出
- JSON 和 Go struct 之间的关系
- `/json` 命令的作用
- 为什么不能只打印模型原始字符串

## 下一关

下一关学习：阶段 1 小结与运行验收。

重点包括：

- 跑通 CLI 的核心命令
- 观察 `/history`、`/config`、`/json`
- 总结阶段 1 的面试表达

## 阶段 1 小结与运行验收

### 本关知识点

阶段 1 的目标不是继续加新功能，而是确认用户能把已经学过的 LLM API 基础串起来：

- LLM API 无状态
- 多轮对话历史
- 流式输出
- 模型参数控制
- 结构化输出
- 基础错误处理

### 本关运行验收

建议用户运行：

```powershell
go run ./01-llm-basics -temperature 0.2 -max-tokens 500
```

然后在 CLI 中测试：

```text
/config
/history
你好，我叫张三
/history
/json RAG 是什么
/reset
/history
/exit
```

### 本关考核要求

本关不问泛后端八股，重点考用户能否把阶段 1 讲成 AI 应用工程经验。

建议问题方向：

- 为什么 LLM 多轮对话需要应用层维护历史
- 为什么历史越长成本和延迟越高
- streaming 解决了什么体验问题
- temperature、max_tokens、model 分别影响什么
- 结构化输出为什么对业务系统重要
- 如何把这个 CLI 描述成一个简历/面试项目

### 当前状态

阶段 1 总考核已收束。

### 本关问答记录

**问题 1：如果面试官问你“LLM 的多轮对话是怎么实现的”，你会怎么回答？**

我的回答：

> 大模型是无状态的，想要做到类似于有状态的能力，LLM 可以把上下文都给到大模型，所以通过 messages 切片做到，messages 有三种类型数据 system 是会话标题、user 是用户问题，assistant 是 AI 的回答。

AI 反馈：

> 整体答得不错，关键点“无状态 + 应用层传上下文 + messages 保存历史”说对了。需要纠正一点：`system` 不是会话标题，而是系统指令，用来设定模型角色、风格、边界和行为规则。

**问题 2：为什么多轮对话越聊越贵、越聊越慢？这和 `messages` 有什么关系？**

我的回答：

> 因为聊越多，上下文就越多，token 也就越多，token 越多使用的钱就越多。messages 就是这个上下文。

AI 反馈：

> 这题答得很好。补充一点：不仅更贵，也可能更慢，因为每次请求都要传更多输入 token，模型也要处理更长上下文；再长还会碰到 context window 上限。

**问题 3：streaming 流式输出解决的主要问题是什么？它是否会让模型最终生成的内容本质上更聪明？**

我的回答：

> streaming 流式输出解决了大模型回答整个问题过慢的问题，因为大模型实际上是逐步推理整个问题的答案的，这个逐步流程很长，把这个过程展示给用户，用户可以知道 AI 是怎么想的，要不要停下来，AI 没有宕机还在思考问题等。这个不会让最终内容变得更聪明。

AI 反馈：

> 答得很好，方向完全对。小修正：streaming 展示的是模型逐步生成的文本，不一定等于“真实思考过程”；但它确实改善等待体验，让用户知道系统还在响应，也方便中途停止。

**问题 4：如果一个功能要求模型输出稳定、可解析的 JSON，你会怎么设置或约束它？**

我的回答：

> temperature 可以设置低一些，可以给 prompt 设置一些提示词，Go 方面要做接受模型输出的详情处理，例如超时、内容解析，错误数据处理等。prompt 这个东西我得理解不到位，你得在后续继续教我。

AI 反馈：

> 答得不错，三个方向都有：低 temperature、prompt 约束、Go 侧解析和错误处理。用户明确提出 prompt 理解还不到位，后续需要专门加强 Prompt 设计。

**问题 5：如果让你把 `01-llm-basics` 这个项目写到简历或面试里，你会怎么介绍它？**

我的回答：

> 目前项目过于简单，我不能整到简历里面，至于面试，前面那么多问题我已经回答了，你后面是不是没有好的问题了？

AI 记录：

> 用户判断正确：阶段 1 项目只是基础 demo，不适合单独写进简历。它的价值是打基础、做面试基础表达、为 RAG 和 Agent 铺路。真正适合简历的是后续阶段 2 RAG 和阶段 3 Agent 项目。AI 后续不应强行把基础 demo 包装成简历项目。

### 阶段 1 结论

阶段 1 已完成。用户已掌握：

- LLM API 无状态
- `messages` 维护多轮上下文
- token 增长会带来成本和延迟增加
- streaming 主要改善体验，不提升模型能力
- 结构化输出需要 prompt 约束、低 temperature、Go 侧解析和错误处理

后续需要加强：

- Prompt 设计
- RAG 项目化能力
- Agent 工具调用能力

## 下一关

下一关进入阶段 2：RAG 检索增强生成。

第一课建议学习：为什么需要 RAG，以及 RAG 和直接问模型、微调的区别。

## Prompt 工程补课记录

### 触发原因

用户指出上一阶段对 Prompt 的理解还不够，而 Prompt 是后续结构化输出、RAG 和 Agent 工具调用的重要基础。因此在继续 RAG 前，先补一课 Prompt 工程。

### 本课知识点

Prompt 不只是“提示词”，而是 LLM 应用里的输入协议。它通常包含：

- 角色：模型应该扮演什么身份
- 任务：模型要完成什么事情
- 上下文：模型应该依据哪些资料
- 输出格式：模型应该返回自然语言、JSON 还是其他结构
- 边界：不能编造、没有答案就说不知道、不要输出 Markdown 等

### 项目落地

已新增笔记：

- `notes/01-prompt-engineering.md`

并结合现有代码讲解了三个 Prompt：

- `01-llm-basics/main.go` 的普通聊天 system prompt
- `/json` 命令里的结构化 JSON prompt
- `02-rag/main.go` 的 RAG prompt

### 本课问答记录

**问题 1：为什么 Prompt 更像“输入协议”，而不是一句普通的提示语？**

用户回答：

> 用来限制大模型的回答的，里面包括角色、任务、上下文、回答格式

AI 反馈：

> 回答正确。Prompt 规定模型这次调用要遵守的输入/输出契约，代码再按这个约定解析和校验。

**问题 2：如果只在 Prompt 里写“请返回 JSON”，但代码里不做 `json.Unmarshal` 校验，真实业务里可能会出什么问题？**

用户回答：

> 会出现代码异常的问题，大模型的回答有时候会出现幻觉，不遵守协议

AI 反馈：

> 回答正确。模型可能返回 Markdown、字段缺失、类型不对或夹杂解释文字。Prompt 是软约束，代码校验是硬约束。

**问题 3：RAG Prompt 里为什么要写“请只根据下面的上下文回答；如果上下文没有答案，就说不知道”？**

用户回答：

> 幻觉，大模型有时候没拿到答案，回答的时候就会使用通用答案或者硬凑个回答

AI 反馈：

> 回答正确。这句话用于把回答边界限制在检索上下文里，避免模型用通用知识硬补企业文档答案。

**问题 4：Prompt 写好了是不是就能保证模型稳定输出正确结果？**

用户回答：

> 不是的，这是软约束，一样可能出现幻觉

AI 反馈：

> 回答正确。Prompt 能提高稳定性，但不能保证正确性，必须结合代码校验、检索验证、错误处理和观测。

### 本课通过标准

本课已通过。用户应能解释：

- Prompt 是输入协议，不只是提示语
- Prompt 通常包含角色、任务、上下文、输出格式和边界
- Prompt 是软约束，代码校验是硬约束
- RAG Prompt 的核心是限制模型只基于检索上下文回答

### 下一步

继续阶段 2：RAG 第一课，重点学习为什么需要 RAG，以及 RAG 和直接问模型、微调的区别。

## RAG 第一课学习记录：为什么需要 RAG

### 本课知识点

RAG 的核心不是让模型永久记住知识，而是在回答前先从外部知识库检索相关内容，再把相关 chunk 放进上下文，让模型基于这些资料回答。

本课重点：

- 直接问模型依赖模型参数里的通用知识，不适合企业私有文档和最新事实
- RAG 适合企业知识库、产品文档、接口说明、规章制度等经常变化的事实知识
- 微调更适合稳定任务模式、回答风格和领域表达，不适合频繁更新事实
- RAG 质量首先受检索质量影响
- Top-K 需要权衡召回率、噪声、成本和延迟

### 项目落地

已新增：

- `02-rag/main.go`：最小 RAG 演示，本地知识库、简单检索、Prompt 拼接、引用来源
- `notes/02-rag-basics.md`：RAG 基础笔记

本课 demo 暂时用关键词打分模拟检索，还没有接真实 Embedding。这样做是为了先理解 RAG 主链路，下一课再升级到向量相似度。

### 运行观察

用户运行了 `02-rag` demo，观察到：

- 不使用 RAG 时，程序无法确认本地项目文档里的具体要求
- 使用 RAG 后，程序先检索出 `notes/01-core-concepts.md` 里的 API Key 配置说明，再基于该上下文回答，并输出来源
- 默认 `top-k=2` 时，可能把 `score=0` 的无关 chunk 也放进 Prompt，说明 Top-K 太大可能带来上下文污染

### 本课问答记录

**问题 1：企业知识库问答为什么通常要用 RAG，而不是直接把问题丢给大模型？**

用户回答：

> 大模型不是直接拥有全部知识然后再等着你问的，而是通过RAG先查阅相关文档，把对应的信息给到上下文，给到大模型，大模型会通过这些信息总结后进行回答

AI 反馈：

> 回答正确。补充企业场景里的知识经常是私有的、最新的，模型训练参数里没有；RAG 可以把答案约束在检索到的文档里，并输出来源。

**问题 2：经常变化的事实知识更适合 RAG 还是微调？**

用户回答：

> RAG，因为如果这个模型是用来和相关业务有关的，那就得告诉他最新的知识，而不是微调

AI 反馈：

> 判断正确。RAG 更新知识库就能生效，成本低、可追溯、可引用；微调更适合改变回答风格、任务格式或稳定行为模式。

**问题 3：RAG 检索排序有什么作用？**

用户回答：

> 给ai的上下文是有限的，所有RAG检索会有个排序，按照排序从小到大，选前面的扔入到ai上下文

AI 反馈：

> 核心正确：上下文有限，所以要排序后选择最相关内容。纠正一点：本 demo 中 score 越高越相关，应按分数从大到小排序。

**问题 4：无关 chunk 塞进 Prompt 会带来什么问题？**

用户回答：

> 幻觉、瞎回答问题

AI 反馈：

> 回答正确。无关 chunk 会导致上下文污染、成本和延迟增加、引用可追溯性变差。

**问题 5：为什么“检索”会直接决定最终回答质量？**

用户回答：

> 因为ai只是根据检索的内容，进行回答问题的，检索的文档不行，回答的质量也就不行

AI 反馈：

> 回答正确。RAG 的回答质量上限首先由检索决定；没有召回正确 chunk，模型只能基于错误或缺失的上下文回答。

**问题 6：企业知识库问答为什么更适合 RAG，而不是直接问模型或把所有知识都拿去微调？**

用户回答：

> 直接问，上下文太少。微调的话，ai回答还是不够准确

AI 反馈：

> 方向正确，但需要更准确：直接问的问题是拿不到企业私有/最新文档；微调的问题不是一定不准确，而是不适合频繁变化的事实知识，成本高、更新慢，也不天然提供引用来源。

标准表达：

> 企业知识库问答更适合 RAG，因为企业文档经常变化，而且很多内容是私有知识，直接问模型拿不到可靠依据；微调更适合学习稳定的风格或任务模式，不适合频繁更新事实。RAG 可以在回答前检索最新文档，把相关 chunk 放进上下文，并返回来源，成本更低、可追溯性更好。

### 本课通过标准

本课已通过。用户应能解释：

- RAG 是回答前检索知识，不是让模型永久记住知识
- RAG 和直接问模型、微调的区别
- 检索质量为什么决定 RAG 回答上限
- Top-K 太小和太大的风险

### 下一步

进入 Embedding 基础：理解 Embedding 是文本语义向量，不是关键词匹配；用 Go 实现余弦相似度，并比较相似文本和不相似文本的向量距离。

### 教学反馈

用户反馈本课学习体验不够好：很多概念没有先在文档中出现，例如 Top-K 是什么。后续课程必须先补齐术语文档，再进入运行观察和问答。

已补充 `notes/02-rag-basics.md`：

- Knowledge Base
- Document
- Chunk
- Retrieve
- Score
- Top-K
- Context
- Source
- 如何看懂 demo 输出
- 本课常见误区

## Embedding 第一课学习记录：文本语义向量与余弦相似度

### 本课知识点

Embedding 是把文本转换成向量，让程序可以用数学方式比较语义相似度。它不是关键词匹配，而是语义检索的基础。

本课重点：

- Embedding 负责把问题和文档 chunk 转成向量
- 向量是一组数字，维度必须一致
- 余弦相似度负责比较两个向量方向是否接近
- 相似度越高，说明语义越可能相关，但不保证答案一定正确
- RAG 会按相似度排序，选择 Top-K chunk 放入上下文

### 项目落地

已新增：

- `notes/03-embedding-basics.md`：Embedding 术语、输出解释和常见误区
- `02-rag/embedding-demo/main.go`：用手写向量演示余弦相似度排序

本课暂时不调用真实 Embedding API，而是先用手写向量讲清楚“问题向量 vs 文档向量 -> 计算相似度 -> 排序”的机制。

### 运行观察

用户运行：

```powershell
go run .\02-rag\embedding-demo
```

观察到：

- 用户问题“如何设置 OpenAI API Key？”的向量与 `API Key 配置` 的向量最接近
- `API Key 配置` 的 similarity=0.994，排第一
- `JSON 结构化输出` 和 `Streaming 流式输出` 相似度更低，排在后面

### 本课问答记录

**问题 1：为什么 `API Key 配置` 排第一？**

用户回答：

> 我们要批评问题和文档的相似度，1先把文本转换成向量，2再计算向量的余弦相识度，相识度越大越精确

AI 反馈：

> 流程正确：文本先转向量，再计算余弦相似度。需要纠正：“相似度越大越精确”不严谨，应该说相似度越大，代表语义越可能相关，但不保证答案一定正确。

**问题 2：为什么说 Embedding 不是关键词匹配？**

用户回答：

> 关键词匹配是相等匹配，向量余弦是相似度。因为关键词匹配常常是不直接等价的，只有是与不是，embedding是相识度

AI 反馈：

> 回答正确。关键词检索更依赖词面，Embedding 把“配置密钥”“设置 API Key”“OPENAI_API_KEY 环境变量”映射到向量空间里，字面不同也可能因为语义接近而被检索出来。

**问题 3：最高相似度 chunk 是否一定包含正确答案？**

用户回答：

> 不能，只是大概率正确，不能做到100%

AI 反馈：

> 回答正确。相似度只是检索相关性信号，不是事实正确性证明。

**问题 4：Embedding 在 RAG 里负责什么？**

用户回答：

> Emdedding就是计算问题和文档的相识度

AI 反馈：

> 接近但需修正：Embedding 负责把问题和文档转换成向量；余弦相似度负责计算这些向量有多接近。

### 本课通过标准

本课已通过。用户应能解释：

- Embedding 是文本到向量的转换
- Embedding 不是关键词匹配
- 余弦相似度用于比较向量方向是否接近
- 相似度高只代表更可能相关，不代表一定正确

### 下一步

调用真实 Embedding API，把两段文本转成真实向量，并比较相似文本和不相似文本的向量相似度。

## Embedding API 课程记录：真实 API 与 mock 模式

### 本课知识点

本课原计划调用真实 Embedding API，把自然语言文本转换成模型向量。但用户没有 OpenAI API 额度，因此改为使用 mock 模式继续学习流程。

本课重点：

- Embedding API 的职责是输入文本、输出向量
- Chat API 的职责是基于 Prompt 和上下文生成自然语言答案
- 余弦相似度通常由代码或向量数据库计算，不是 Chat API 的职责
- 用户问题向量和文档 chunk 向量必须来自同一个 Embedding 模型
- 换 Embedding 模型后，文档库里的旧向量通常要重新生成

### 项目落地

已新增：

- `notes/04-embedding-api.md`：Embedding API 术语、运行方式、mock 模式说明
- `02-rag/embedding-api/main.go`：支持真实 API 和 `-mock` 本地模拟模式

运行 mock 模式：

```powershell
go run .\02-rag\embedding-api -mock
```

mock 模式不会联网，也不需要 API Key。它用固定向量模拟 Embedding API 返回结果，保留“文本 -> 向量 -> 余弦相似度 -> 判断语义相关性”的工程流程。

### 运行观察

用户运行 mock 模式后观察到：

- 文本 1：“怎么配置 OpenAI API Key？”
- 文本 2：“如何设置 OPENAI_API_KEY 环境变量？”
- 文本 3：“流式输出可以改善用户等待体验。”

相似度结果：

```text
文本 1 vs 文本 2 = 0.9953
文本 1 vs 文本 3 = 0.1494
```

说明 API Key 配置相关的两段文本向量方向更接近；Streaming 主题不同，相似度更低。

### 本课问答记录

**问题 1：为什么文本 1 vs 文本 2 的相似度比文本 1 vs 文本 3 高很多？**

用户回答：

> 因为文本1到文本2的向量余弦相识度大于文本1到文本3的向量余弦相识度，余弦相识度越接近1🈷相识

AI 反馈：

> 回答正确。补充：文本 1 和文本 2 语义相近，都在讲 API Key 配置，所以向量方向接近，余弦相似度接近 1；文本 3 讲 Streaming，语义不同，所以相似度低。

**问题 2：为什么用户问题向量和文档 chunk 向量必须用同一个 Embedding 模型生成？**

用户回答：

> embedding不同模型，计算的向量余弦相识度就会有差别

AI 反馈：

> 方向正确。更准确地说，不同 Embedding 模型生成的是不同向量空间，坐标含义不一致，直接计算相似度没有可靠意义。

**问题 3：Embedding API 和 Chat API 的职责有什么区别？**

用户回答：

> embedding模型只是用来转换文本成向量和计算向量余弦相似度的。chat的职责是拿这些余弦相识度进行再处理？

AI 反馈：

> 前半句基本正确，但需修正：Embedding API 负责把文本转成向量；余弦相似度通常由代码或向量数据库计算。Chat API 的职责是基于 Prompt 和检索上下文生成自然语言答案，不是处理相似度。

**问题 4：换 Embedding 模型后，为什么文档库里的旧向量通常要重新生成？**

用户回答：

> 和之前的问题是一样，只能使用同一种embedding模型，不同模型生成的向量会不同

AI 反馈：

> 回答正确。同一个检索系统里，查询向量和文档向量必须在同一个向量空间。换模型后旧文档向量和新查询向量不再可靠可比，所以通常要重建文档向量。

### 本课状态

本课 mock 模式已通过。真实 Embedding API 调用代码已实现，但因为用户没有 API 额度，真实 API 验证暂时保留为未完成。

### 下一步

进入文档处理：读取本地 Markdown 文档，把长文档切成 chunk，并为每个 chunk 保存 source、title、content、index 等元数据。
