# 阶段 2 学习记录：RAG 检索增强生成

> 从原 `AI_TUTORING.md` 拆分而来，包含进入 RAG 前的 Prompt 工程补课。

## Prompt 工程补课记录

### 触发原因

用户指出上一阶段对 Prompt 的理解还不够，而 Prompt 是后续结构化输出、RAG 和 Agent 工具调用的重要基础。因此在继续 RAG 前，先补一课 Prompt 工程。

### 本课知识点

Prompt 不只是“提示词”，而是 LLM 应用里的输入协议。它通常包含：

- 角色：模型应该扮演什么身份
- 任务：模型要完成什么事情
- 上下文：模型应该依据哪些资料
- 输出格式：模型应该返回自然语言、JSON 还是其他结构
- 边界：不能编造、没有答案就说不知道、不要输出 Markdown 等

### 项目落地

已新增笔记：

- `notes/01-prompt-engineering.md`

并结合现有代码讲解了三个 Prompt：

- `01-llm-basics/main.go` 的普通聊天 system prompt
- `/json` 命令里的结构化 JSON prompt
- `02-rag/main.go` 的 RAG prompt

### 本课问答记录

**问题 1：为什么 Prompt 更像“输入协议”，而不是一句普通的提示语？**

用户回答：

> 用来限制大模型的回答的，里面包括角色、任务、上下文、回答格式

AI 反馈：

> 回答正确。Prompt 规定模型这次调用要遵守的输入/输出契约，代码再按这个约定解析和校验。

**问题 2：如果只在 Prompt 里写“请返回 JSON”，但代码里不做 `json.Unmarshal` 校验，真实业务里可能会出什么问题？**

用户回答：

> 会出现代码异常的问题，大模型的回答有时候会出现幻觉，不遵守协议

AI 反馈：

> 回答正确。模型可能返回 Markdown、字段缺失、类型不对或夹杂解释文字。Prompt 是软约束，代码校验是硬约束。

**问题 3：RAG Prompt 里为什么要写“请只根据下面的上下文回答；如果上下文没有答案，就说不知道”？**

用户回答：

> 幻觉，大模型有时候没拿到答案，回答的时候就会使用通用答案或者硬凑个回答

AI 反馈：

> 回答正确。这句话用于把回答边界限制在检索上下文里，避免模型用通用知识硬补企业文档答案。

**问题 4：Prompt 写好了是不是就能保证模型稳定输出正确结果？**

用户回答：

> 不是的，这是软约束，一样可能出现幻觉

AI 反馈：

> 回答正确。Prompt 能提高稳定性，但不能保证正确性，必须结合代码校验、检索验证、错误处理和观测。

### 本课通过标准

本课已通过。用户应能解释：

- Prompt 是输入协议，不只是提示语
- Prompt 通常包含角色、任务、上下文、输出格式和边界
- Prompt 是软约束，代码校验是硬约束
- RAG Prompt 的核心是限制模型只基于检索上下文回答

### 下一步

继续阶段 2：RAG 第一课，重点学习为什么需要 RAG，以及 RAG 和直接问模型、微调的区别。

## RAG 第一课学习记录：为什么需要 RAG

### 本课知识点

RAG 的核心不是让模型永久记住知识，而是在回答前先从外部知识库检索相关内容，再把相关 chunk 放进上下文，让模型基于这些资料回答。

本课重点：

- 直接问模型依赖模型参数里的通用知识，不适合企业私有文档和最新事实
- RAG 适合企业知识库、产品文档、接口说明、规章制度等经常变化的事实知识
- 微调更适合稳定任务模式、回答风格和领域表达，不适合频繁更新事实
- RAG 质量首先受检索质量影响
- Top-K 需要权衡召回率、噪声、成本和延迟

### 项目落地

已新增：

- `02-rag/main.go`：最小 RAG 演示，本地知识库、简单检索、Prompt 拼接、引用来源
- `notes/02-rag-basics.md`：RAG 基础笔记

本课 demo 暂时用关键词打分模拟检索，还没有接真实 Embedding。这样做是为了先理解 RAG 主链路，下一课再升级到向量相似度。

### 运行观察

用户运行了 `02-rag` demo，观察到：

- 不使用 RAG 时，程序无法确认本地项目文档里的具体要求
- 使用 RAG 后，程序先检索出 `notes/01-core-concepts.md` 里的 API Key 配置说明，再基于该上下文回答，并输出来源
- 默认 `top-k=2` 时，可能把 `score=0` 的无关 chunk 也放进 Prompt，说明 Top-K 太大可能带来上下文污染

### 本课问答记录

**问题 1：企业知识库问答为什么通常要用 RAG，而不是直接把问题丢给大模型？**

用户回答：

> 大模型不是直接拥有全部知识然后再等着你问的，而是通过RAG先查阅相关文档，把对应的信息给到上下文，给到大模型，大模型会通过这些信息总结后进行回答

AI 反馈：

> 回答正确。补充企业场景里的知识经常是私有的、最新的，模型训练参数里没有；RAG 可以把答案约束在检索到的文档里，并输出来源。

**问题 2：经常变化的事实知识更适合 RAG 还是微调？**

用户回答：

> RAG，因为如果这个模型是用来和相关业务有关的，那就得告诉他最新的知识，而不是微调

AI 反馈：

> 判断正确。RAG 更新知识库就能生效，成本低、可追溯、可引用；微调更适合改变回答风格、任务格式或稳定行为模式。

**问题 3：RAG 检索排序有什么作用？**

用户回答：

> 给ai的上下文是有限的，所有RAG检索会有个排序，按照排序从小到大，选前面的扔入到ai上下文

AI 反馈：

> 核心正确：上下文有限，所以要排序后选择最相关内容。纠正一点：本 demo 中 score 越高越相关，应按分数从大到小排序。

**问题 4：无关 chunk 塞进 Prompt 会带来什么问题？**

用户回答：

> 幻觉、瞎回答问题

AI 反馈：

> 回答正确。无关 chunk 会导致上下文污染、成本和延迟增加、引用可追溯性变差。

**问题 5：为什么“检索”会直接决定最终回答质量？**

用户回答：

> 因为ai只是根据检索的内容，进行回答问题的，检索的文档不行，回答的质量也就不行

AI 反馈：

> 回答正确。RAG 的回答质量上限首先由检索决定；没有召回正确 chunk，模型只能基于错误或缺失的上下文回答。

**问题 6：企业知识库问答为什么更适合 RAG，而不是直接问模型或把所有知识都拿去微调？**

用户回答：

> 直接问，上下文太少。微调的话，ai回答还是不够准确

AI 反馈：

> 方向正确，但需要更准确：直接问的问题是拿不到企业私有/最新文档；微调的问题不是一定不准确，而是不适合频繁变化的事实知识，成本高、更新慢，也不天然提供引用来源。

标准表达：

> 企业知识库问答更适合 RAG，因为企业文档经常变化，而且很多内容是私有知识，直接问模型拿不到可靠依据；微调更适合学习稳定的风格或任务模式，不适合频繁更新事实。RAG 可以在回答前检索最新文档，把相关 chunk 放进上下文，并返回来源，成本更低、可追溯性更好。

### 本课通过标准

本课已通过。用户应能解释：

- RAG 是回答前检索知识，不是让模型永久记住知识
- RAG 和直接问模型、微调的区别
- 检索质量为什么决定 RAG 回答上限
- Top-K 太小和太大的风险

### 下一步

进入 Embedding 基础：理解 Embedding 是文本语义向量，不是关键词匹配；用 Go 实现余弦相似度，并比较相似文本和不相似文本的向量距离。

### 教学反馈

用户反馈本课学习体验不够好：很多概念没有先在文档中出现，例如 Top-K 是什么。后续课程必须先补齐术语文档，再进入运行观察和问答。

已补充 `notes/02-rag-basics.md`：

- Knowledge Base
- Document
- Chunk
- Retrieve
- Score
- Top-K
- Context
- Source
- 如何看懂 demo 输出
- 本课常见误区

## Embedding 第一课学习记录：文本语义向量与余弦相似度

### 本课知识点

Embedding 是把文本转换成向量，让程序可以用数学方式比较语义相似度。它不是关键词匹配，而是语义检索的基础。

本课重点：

- Embedding 负责把问题和文档 chunk 转成向量
- 向量是一组数字，维度必须一致
- 余弦相似度负责比较两个向量方向是否接近
- 相似度越高，说明语义越可能相关，但不保证答案一定正确
- RAG 会按相似度排序，选择 Top-K chunk 放入上下文

### 项目落地

已新增：

- `notes/03-embedding-basics.md`：Embedding 术语、输出解释和常见误区
- `02-rag/embedding-demo/main.go`：用手写向量演示余弦相似度排序

本课暂时不调用真实 Embedding API，而是先用手写向量讲清楚“问题向量 vs 文档向量 -> 计算相似度 -> 排序”的机制。

### 运行观察

用户运行：

```powershell
go run .\02-rag\embedding-demo
```

观察到：

- 用户问题“如何设置 OpenAI API Key？”的向量与 `API Key 配置` 的向量最接近
- `API Key 配置` 的 similarity=0.994，排第一
- `JSON 结构化输出` 和 `Streaming 流式输出` 相似度更低，排在后面

### 本课问答记录

**问题 1：为什么 `API Key 配置` 排第一？**

用户回答：

> 我们要批评问题和文档的相似度，1先把文本转换成向量，2再计算向量的余弦相识度，相识度越大越精确

AI 反馈：

> 流程正确：文本先转向量，再计算余弦相似度。需要纠正：“相似度越大越精确”不严谨，应该说相似度越大，代表语义越可能相关，但不保证答案一定正确。

**问题 2：为什么说 Embedding 不是关键词匹配？**

用户回答：

> 关键词匹配是相等匹配，向量余弦是相似度。因为关键词匹配常常是不直接等价的，只有是与不是，embedding是相识度

AI 反馈：

> 回答正确。关键词检索更依赖词面，Embedding 把“配置密钥”“设置 API Key”“OPENAI_API_KEY 环境变量”映射到向量空间里，字面不同也可能因为语义接近而被检索出来。

**问题 3：最高相似度 chunk 是否一定包含正确答案？**

用户回答：

> 不能，只是大概率正确，不能做到100%

AI 反馈：

> 回答正确。相似度只是检索相关性信号，不是事实正确性证明。

**问题 4：Embedding 在 RAG 里负责什么？**

用户回答：

> Emdedding就是计算问题和文档的相识度

AI 反馈：

> 接近但需修正：Embedding 负责把问题和文档转换成向量；余弦相似度负责计算这些向量有多接近。

### 本课通过标准

本课已通过。用户应能解释：

- Embedding 是文本到向量的转换
- Embedding 不是关键词匹配
- 余弦相似度用于比较向量方向是否接近
- 相似度高只代表更可能相关，不代表一定正确

### 下一步

调用真实 Embedding API，把两段文本转成真实向量，并比较相似文本和不相似文本的向量相似度。

## Embedding API 课程记录：真实 API 与 mock 模式

### 本课知识点

本课原计划调用真实 Embedding API，把自然语言文本转换成模型向量。但用户没有 OpenAI API 额度，因此改为使用 mock 模式继续学习流程。

本课重点：

- Embedding API 的职责是输入文本、输出向量
- Chat API 的职责是基于 Prompt 和上下文生成自然语言答案
- 余弦相似度通常由代码或向量数据库计算，不是 Chat API 的职责
- 用户问题向量和文档 chunk 向量必须来自同一个 Embedding 模型
- 换 Embedding 模型后，文档库里的旧向量通常要重新生成

### 项目落地

已新增：

- `notes/04-embedding-api.md`：Embedding API 术语、运行方式、mock 模式说明
- `02-rag/embedding-api/main.go`：支持真实 API 和 `-mock` 本地模拟模式

运行 mock 模式：

```powershell
go run .\02-rag\embedding-api -mock
```

mock 模式不会联网，也不需要 API Key。它用固定向量模拟 Embedding API 返回结果，保留“文本 -> 向量 -> 余弦相似度 -> 判断语义相关性”的工程流程。

### 运行观察

用户运行 mock 模式后观察到：

- 文本 1：“怎么配置 OpenAI API Key？”
- 文本 2：“如何设置 OPENAI_API_KEY 环境变量？”
- 文本 3：“流式输出可以改善用户等待体验。”

相似度结果：

```text
文本 1 vs 文本 2 = 0.9953
文本 1 vs 文本 3 = 0.1494
```

说明 API Key 配置相关的两段文本向量方向更接近；Streaming 主题不同，相似度更低。

### 本课问答记录

**问题 1：为什么文本 1 vs 文本 2 的相似度比文本 1 vs 文本 3 高很多？**

用户回答：

> 因为文本1到文本2的向量余弦相识度大于文本1到文本3的向量余弦相识度，余弦相识度越接近1🈷相识

AI 反馈：

> 回答正确。补充：文本 1 和文本 2 语义相近，都在讲 API Key 配置，所以向量方向接近，余弦相似度接近 1；文本 3 讲 Streaming，语义不同，所以相似度低。

**问题 2：为什么用户问题向量和文档 chunk 向量必须用同一个 Embedding 模型生成？**

用户回答：

> embedding不同模型，计算的向量余弦相识度就会有差别

AI 反馈：

> 方向正确。更准确地说，不同 Embedding 模型生成的是不同向量空间，坐标含义不一致，直接计算相似度没有可靠意义。

**问题 3：Embedding API 和 Chat API 的职责有什么区别？**

用户回答：

> embedding模型只是用来转换文本成向量和计算向量余弦相似度的。chat的职责是拿这些余弦相识度进行再处理？

AI 反馈：

> 前半句基本正确，但需修正：Embedding API 负责把文本转成向量；余弦相似度通常由代码或向量数据库计算。Chat API 的职责是基于 Prompt 和检索上下文生成自然语言答案，不是处理相似度。

**问题 4：换 Embedding 模型后，为什么文档库里的旧向量通常要重新生成？**

用户回答：

> 和之前的问题是一样，只能使用同一种embedding模型，不同模型生成的向量会不同

AI 反馈：

> 回答正确。同一个检索系统里，查询向量和文档向量必须在同一个向量空间。换模型后旧文档向量和新查询向量不再可靠可比，所以通常要重建文档向量。

### 本课状态

本课 mock 模式已通过。真实 Embedding API 调用代码已实现，但因为用户没有 API 额度，真实 API 验证暂时保留为未完成。

### 下一步

进入文档处理：读取本地 Markdown 文档，把长文档切成 chunk，并为每个 chunk 保存 source、title、content、index 等元数据。

## 文档处理课程记录：Markdown 读取与 chunk 切分

### 本课知识点

文档处理是 RAG 建库的第一步。真实 RAG 不会把整篇文档直接塞给模型，而是先切成 chunk，并为每个 chunk 保存 metadata。

本课重点：

- Markdown 是适合学习 RAG 的纯文本知识库格式
- chunk 是后续 Embedding 和检索的基本单位
- metadata 包括 source、title、index、content，用于来源引用和问题排查
- 按标题切分能保留语义结构，但 chunk 大小不稳定
- 固定长度切分大小稳定，但可能切断句子、代码块或表格
- chunk 太大容易带入无关内容，chunk 太小可能缺少上下文

### 项目落地

已新增：

- `notes/05-document-processing.md`：文档处理术语、切分策略、输出解释和常见误区
- `02-rag/chunk-demo/main.go`：读取 Markdown，支持 `heading` 和 `fixed` 两种切分策略

运行方式：

```powershell
go run .\02-rag\chunk-demo -mode heading
go run .\02-rag\chunk-demo -mode fixed -chunk-size 500
```

### 运行观察

用户运行按标题切分：

- 初始输出出现了只有标题、没有正文的低质量 chunk
- 已改进代码：跳过只有标题没有正文的 chunk
- 改进后 `notes\02-rag-basics.md` 被切成 23 个 heading chunk

用户运行固定长度切分：

- `chunk-size=500` 时切出 10 个 chunk
- 每个 chunk 大小更稳定
- 但 Chunk #3 从代码片段中间开始，说明固定长度切分可能破坏语义结构

### 本课问答记录

**问题 1：按标题切分和固定长度切分各有什么优点和问题？**

用户回答：

> 按标题切，chunk的内容大小差别比较大。按固定长度切，虽然处理的内容长度的问题，但是可以会有内容不全的问题

AI 反馈：

> 回答正确。按标题切分语义结构更完整，但 chunk 大小不稳定；固定长度切分大小稳定，但可能从句子、代码块、表格中间切开，导致上下文不完整。

**问题 2：为什么每个 chunk 不能只保存 content，还要保存 source、title、index 这些 metadata？**

用户回答：

> 因为最终的答案，需要回答来源于哪里

AI 反馈：

> 回答正确。metadata 用于来源引用、错误排查和前端展示。向量库里不能只存 embedding，也要存 source、title、index、content 等元数据。

**问题 3：chunk 太大或太小分别会造成什么影响？**

用户回答：

> 太大可能会有无效信息，浪费金钱。太小可能会内容不全

AI 反馈：

> 回答正确。chunk 太大容易带入无关内容，增加 token 成本和延迟，也可能污染上下文；chunk 太小可能缺少必要上下文，导致检索到了片段也无法回答完整问题。

### 本课通过标准

本课已通过。用户应能解释：

- 为什么 RAG 要把文档切成 chunk
- metadata 为什么重要
- 标题切分和固定长度切分的取舍
- chunk size 如何影响成本、召回、噪声和上下文完整性

### 下一步

进入内存向量库：把 chunk 和向量保存到内存结构里，支持 Top-K 相似度检索。

## 内存向量库课程记录：写入 chunk 并做 Top-K 检索

### 本课知识点

向量库负责保存 chunk 的 embedding、content 和 metadata，并在查询时根据用户问题向量返回最相关的 Top-K chunk。

本课重点：

- 向量库不能只存 vector，还要存 content 和 metadata
- content 用于后续拼 Prompt
- metadata 用于来源引用、前端展示和问题排查
- 内存向量库适合学习和小数据量，不适合大规模生产
- 本课使用暴力检索：遍历所有向量，计算相似度，排序后返回 Top-K
- Top-K 越大，召回更多，但也更容易带入弱相关或无关 chunk

### 项目落地

已新增：

- `notes/06-vector-store.md`：向量库术语、Top-K 检索、暴力检索和常见误区
- `02-rag/vector-store-demo/main.go`：内存向量库，支持写入 chunk 向量和 Top-K 检索

运行方式：

```powershell
go run .\02-rag\vector-store-demo
go run .\02-rag\vector-store-demo -top-k 3
```

### 运行观察

用户运行默认 `top-k=2`：

```text
1. score=0.994 title=API Key 配置
2. score=0.518 title=RAG 和微调的区别
```

说明最相关的 API Key chunk 排第一，第二条已经弱相关。

用户运行 `top-k=3`：

```text
1. score=0.994 title=API Key 配置
2. score=0.518 title=RAG 和微调的区别
3. score=0.226 title=JSON 结构化输出
```

说明 Top-K 增大后会召回更多 chunk，但第 2、3 条和问题“怎么设置 OpenAI API Key？”弱相关或无关，可能污染 Prompt。

### 本课问答记录

**问题 1：内存向量库里每条记录为什么不能只存 vector，还要存 content 和 metadata？**

用户回答：

> 因为回答用户问题后要说明来源

AI 反馈：

> 回答正确。补充：vector 只用于相似度检索，content 用于拼 Prompt，source/title/index 用于来源引用和问题排查。

**问题 2：Top-K 变大有什么好处和风险？**

用户回答：

> 上下文会更完整，但是有可能是垃圾信息

AI 反馈：

> 回答正确。Top-K 变大可以召回更多候选 chunk，降低漏掉答案的概率；但也会带入弱相关或无关 chunk，增加 token 成本和延迟，并可能导致模型答偏。

**问题 3：为什么内存暴力检索适合学习和小数据量，但不适合大规模生产？**

用户回答：

> 感觉是每次都要遍历计算两个向量的相似度，计算量估计会特别的大

AI 反馈：

> 回答正确。暴力检索每次查询都要和所有向量计算相似度，时间复杂度接近 O(n)。生产大规模 RAG 通常使用向量数据库和索引结构，例如 HNSW。

**问题 4：如果面试官问“RAG 系统里的向量库负责什么”，怎么回答？**

用户回答：

> 负责保存向量数据，包括内容、标题、原路径等

AI 反馈：

> 方向正确。标准表达：向量库负责保存每个 chunk 的 embedding、content 和 metadata，并在查询时根据用户问题向量做 Top-K 相似度检索，返回最相关的 chunk。content 用来拼 Prompt，metadata 用来做来源引用和排查问题。

### 本课通过标准

本课已通过。用户应能解释：

- 向量库存储 embedding、content 和 metadata
- Top-K 检索的输入、排序和输出
- 为什么 Top-K 不是越大越好
- 内存暴力检索和真实向量数据库的区别

### 下一步

进入 RAG 问答链路：把“问题 -> 向量检索 -> Top-K chunk -> Prompt 拼接 -> 回答”串成一个最小闭环。真实向量数据库 Qdrant/pgvector 暂时后置。

## RAG 问答链路课程记录：从问题到带来源答案

### 本课知识点

本课把前面几课串成最小 RAG 闭环：

```text
用户问题 -> query embedding -> Top-K 检索 -> Prompt 拼接 -> 基于上下文回答 -> 输出来源
```

本课重点：

- Retrieval 负责从向量库找候选 chunk
- Context 是拼进 Prompt 给模型参考的检索结果
- Prompt Assembly 把系统规则、上下文和用户问题组织成模型输入
- Grounded Answer 要求答案基于上下文
- Citation 用于来源引用和错误排查
- Score Threshold 用于判断上下文是否足够相关，不相关时应说不知道

### 项目落地

已新增：

- `notes/07-rag-chain.md`：RAG 问答链路术语、输出解释和常见误区
- `02-rag/rag-chain-demo/main.go`：mock RAG 闭环，支持 Top-K、Prompt 拼接、来源引用和不知道场景

运行方式：

```powershell
go run .\02-rag\rag-chain-demo
go run .\02-rag\rag-chain-demo -question "Go goroutine 怎么调度？"
```

### 运行观察

用户运行默认问题“怎么设置 OpenAI API Key？”：

- query vector 命中 API Key 配置
- Top-1 score=0.994，高于 threshold=0.75
- Prompt 中包含 Top-K context
- 最终回答使用 API Key chunk，并输出来源：`notes\01-core-concepts.md / API Key 配置 / chunk #1`

用户运行未知问题“Go goroutine 怎么调度？”：

- Top-K 仍返回相对最高的 Streaming 和 JSON chunk
- 但 Top-1 score=-0.055，低于 threshold=0.75
- 最终回答：`根据当前知识库上下文，我不知道答案。`

这说明 Top-K 总会返回“相对最接近”的候选，但不代表候选一定能回答问题。

### 本课问答记录

**问题 1：为什么 RAG 不是“只要检索到 chunk 就回答”，而是还要看相似度阈值或上下文是否足够相关？**

用户回答：

> 这样回答不好，因为chunk里面可能会有更好的答案，你不能随便整个问题答案

AI 反馈：

> 方向是对的：不能随便拿 chunk 回答。更准确地说，Top-K 永远会返回相对最接近的几个 chunk，即使它们整体都不相关。低于阈值时应说不知道，避免模型基于无关内容硬编。

**重复问题反馈**

AI 追问了“为什么 Prompt 里要拼 source/title/index/content”，用户指出这个问题已经问太多次。后续应避免重复考 metadata 来源引用问题，除非进入新的工程场景。

**问题 2：为什么真实 RAG 不能直接把 Top-K 结果原样返回给用户，而还需要 Chat Model 基于 Prompt 生成答案？**

用户回答：

> Top-K的数据是文档信息，Chat的回答才是最终的答案

AI 反馈：

> 回答正确。Top-K 返回的是候选资料，不是面向用户的最终表达。Chat Model 的作用是把检索资料组织成自然语言答案，并按 Prompt 规则控制边界。

**问题 3：如果面试官让你用一句话讲清楚 RAG 的完整链路，你会怎么说？**

用户回答：

> chunk里面保存了文档信息，embedding可以把文本信息转成向量和计算向量余弦相识度，top-k可以获取到向量相识的前几名文档给到chat ai，chat ai根据上下文计算出最终答案

AI 反馈：

> 链路已经串起来了，但需修正：Embedding 负责把文本转向量，相似度通常由代码或向量库计算；Chat Model 是基于上下文生成答案，不是“计算出答案”。

标准表达：

> RAG 会先把用户问题转成 query embedding，到向量库里做 Top-K 相似度检索，取出最相关的 chunk 和 metadata，再把这些上下文拼进 Prompt，让 Chat Model 基于上下文生成答案并返回引用来源；如果上下文不相关，就明确说不知道。

### 本课通过标准

本课已通过。用户应能解释：

- RAG 问答链路不是一次模型调用，而是检索和生成的组合
- Top-K 结果是候选上下文，不是最终答案
- Prompt 拼接用于约束模型只基于上下文回答
- 阈值可以避免无关上下文导致模型硬答
- 最终答案应带引用来源；无答案时应明确说不知道

### 下一步

进入 RAG 调优与面试准备：调整 Top-K、观察噪声变化，对比无 RAG 和有 RAG 的效果，总结召回失败、上下文污染、幻觉和引用错误。

## RAG 调优与面试准备课程记录：进行中

### 本课知识点

本课从“能跑通 RAG”进入“能调优 RAG”。重点不是继续堆功能，而是理解真实项目里答案不好时应该如何定位问题：

- `Top-K`：召回更多候选 chunk 与引入噪声、成本、延迟之间的权衡
- `threshold`：回答率与错误率之间的权衡
- `chunk size`：上下文完整性、主题纯度和 token 成本之间的权衡
- 无 RAG vs 有 RAG：是否检索本地知识库、是否有引用来源、是否能在无答案时说不知道
- 常见问题：召回失败、上下文污染、幻觉、引用错误

### 项目落地

已新增：

- `notes/08-rag-tuning.md`：RAG 调优术语、输出解释、排查顺序和面试表达模板
- `02-rag/rag-tuning-demo/main.go`：mock RAG 调优实验，集中观察 Top-K、threshold、chunk size、无 RAG/有 RAG 对比

运行方式：

```powershell
go run .\02-rag\rag-tuning-demo
go run .\02-rag\rag-tuning-demo -question "Go goroutine 怎么调度？"
go run .\02-rag\rag-tuning-demo -top-k 4 -threshold 0.50
```

### 当前状态

AI 已验证 demo 可运行：

- 默认问题“怎么设置 OpenAI API Key？”可以召回 `API Key 配置` 并输出来源
- 知识库外问题“Go goroutine 怎么调度？”会因为 Top-1 分数低于阈值而说不知道

### 下一步

用户运行 `rag-tuning-demo`，观察输出后进入本课问答。问答应避免重复 metadata 来源问题，重点考察 RAG 调优、排查顺序和面试表达。

### 本课问答记录

**问题 1：如果 `Top-K` 从 1 调到 4，输出里 `chars` 和 `noise` 都变大，这说明 RAG 系统发生了什么？这对答案质量、成本和延迟分别有什么影响？**

用户回答：

> chars变大，上下文更大。noise变大，无关信息变多

AI 反馈：

> 回答正确。补充成工程表达：Top-K 变大会召回更多 chunk，可能减少漏召回，但也会增加 Prompt 长度、token 成本、延迟和上下文污染风险。

**问题 2：如果 Top-1 score 很低，低于 threshold，为什么应该说不知道，而不是把 chunk 交给模型硬答？**

用户回答：

> scoure太低表面知识库里面数据不适合回答用户的问题

AI 反馈：

> 判断正确。修正表达：是 `score`；更准确地说，是当前检索结果没有找到足够相关的上下文。可能是知识库没有答案，也可能是检索失败、chunk 切分、Embedding 或 Top-K 参数问题。

**问题 3：如果 RAG 系统回答错了，怎么判断是检索阶段问题，还是生成阶段问题？**

用户回答：

> 1embedding的模型是否是专门用来处理文字的  
> 2知识库里面没有问题的相关答案？  
> 3chunk切太小可能是内容不全，chunk太大无用信息太大  
> 4top-k检索数量太少错过答案，检索数量太大，无用信息也多

AI 反馈：

> 用户列出了检索侧的主要排查原因。补充生成侧判断：如果 Top-K 已经召回正确 chunk，且 chunk 里有答案，但最终答案仍然编造、漏答、答偏或引用错误，则更可能是生成阶段、Prompt、temperature、上下文组织或模型能力问题。

**问题 4：为什么 chunk size 不是越大越好，也不是越小越好？**

用户回答：

> 1太大有垃圾信息，太小错过信息  
> 2太大噪声多  
> 3太大token多  
> 4

AI 反馈：

> 用户掌握了答案完整性、噪声、token 成本和延迟的影响。补充第 4 点：chunk size 还会影响 Embedding 表示和向量检索效果。太小语义不足，太大主题混杂、向量表示被稀释。

**问题 5：RAG 项目上线后，用户反馈答案不准，你会怎么排查？**

用户回答：

> 1embedding的模型可能会有问题  
> 2top-K 太大太小都可以会对问题的结果造成影响  
> 3chunk内容不能太大也不能太小  
> 4prompt。。  
> 5最终答案可能还是幻觉了  
> 6引用来源可能是错误的

AI 反馈：

> 方向正确，但需要从“列原因”升级为“按链路排查”：先看用户问题和日志，再看 query embedding、Top-K 是否召回正确 chunk、chunk 是否完整、Prompt 是否约束只基于上下文回答、最终答案是否忠实上下文、引用来源是否一致，最后把问题加入评测集。

**问题 6：Top-K 召回了正确 chunk，chunk 内容也有答案，但最终回答仍编造上下文里没有的信息，这是检索问题还是生成问题？怎么优化？**

用户回答：

> 像生成问题，会整一下评测集来控制ai的输出

AI 反馈：

> 判断正确：这更像生成阶段问题。修正表达：评测集不是直接控制模型输出，而是用于验证 Prompt、模型、Top-K、threshold 等改动是否降低幻觉率。真正控制输出的是 Prompt 约束、低 temperature、答案校验、引用校验、必要时更换模型或增加后处理。

**重复问题反馈**

AI 继续追问“RAG 和直接问大模型相比，为什么更适合企业知识库问答”，用户指出：

> 这问题已经问过了

AI 记录：

> 用户反馈正确。该问题已在 RAG 第一课中问过，不应在 RAG 调优课重复。后续考核应减少重复概念题，更多关注新场景：调参排查、评测集、日志观测、召回失败和生成失败的边界判断。

### 本课通过标准

本课已收束。用户已能解释：

- Top-K 变大会增加上下文长度和噪声，不是越大越好
- score 低于 threshold 时应说不知道，避免硬答和幻觉
- RAG 排查要先判断检索是否召回正确 chunk，再判断生成是否忠实上下文
- chunk size 需要平衡答案完整性、噪声、成本、延迟和向量检索效果
- 召回正确但最终编造，通常更像生成阶段问题

后续需要继续加强：

- 把排查点组织成更像线上工程流程的表达，而不是只列原因
- 评测集、日志、可观测性在 RAG 调优中的作用
- 避免重复回答已经掌握的基础问题

### 下一步

阶段 2 RAG 主线已完成基础闭环和调优入门。下一课进入阶段 3：Function Calling 与 AI Agent，重点学习 tool schema / function schema 的作用，以及模型如何决定调用工具。

